import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile">
      <h1>Profile Component</h1>
    </Base>
  );
};

export default Profile;
